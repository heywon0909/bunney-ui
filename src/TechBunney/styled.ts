import styled from '@emotion/styled'

export const TechWrapper = styled.div`
    display: flex;
    flex-direction: column;
    position: relative;
    justify-content: center;
    align-items: center;
`
export const MacWrapper = styled.div`
    position: absolute;
    top: 69%;
`
